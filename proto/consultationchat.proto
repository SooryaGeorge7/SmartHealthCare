syntax = "proto3";

package chat;



service ChatService {
  rpc ConsultationChat (stream PatientMessage) returns (stream DoctorMessage);
}

message PatientMessage {
  string message = 1;
}

message DoctorMessage {
  string response =1;
}

